#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: int that must be added to b
 * @b: int that must be added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
